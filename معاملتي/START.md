# البدء السريع 🚀

## متطلبات النظام

- Windows 10/11
- Node.js 18+
- PostgreSQL 14+
- PowerShell

## الخطوات

### 1️⃣ إعداد قاعدة البيانات

افتح PowerShell وقم بتشغيل:

```powershell
# إنشاء قاعدة البيانات
createdb moamalaty_db

# الانتقال لمجلد database
cd "C:\Users\dell\Desktop\معاملتي\database"

# تشغيل السكريبتات
psql -d moamalaty_db -f schema.sql
psql -d moamalaty_db -f seed.sql
```

### 2️⃣ تشغيل Backend

في PowerShell جديد:

```powershell
cd "C:\Users\dell\Desktop\معاملتي\backend"

# تثبيت المكتبات
npm install

# نسخ ملف البيئة
copy .env.example .env

# ⚠️ مهم: افتح .env وغير DB_PASSWORD
# DB_PASSWORD=كلمة_مرور_postgres_الخاصة_بك

# إنشاء المستخدمين التجريبيين
npm run create-users

# تشغيل الخادم
npm run dev
```

✅ الخادم يعمل على: http://localhost:5000

### 3️⃣ تشغيل Frontend

في PowerShell جديد:

```powershell
cd "C:\Users\dell\Desktop\معاملتي\frontend"

# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm run dev
```

✅ التطبيق يعمل على: http://localhost:5173

### 4️⃣ افتح المتصفح

انتقل إلى: **http://localhost:5173**

## حسابات التجربة 🔑

| الدور | البريد | كلمة المرور |
|------|--------|-------------|
| مواطن | citizen@test.com | Test123! |
| موظف | employee@gov.iq | Test123! |
| مستشار | lawyer@moamalaty.iq | Test123! |
| مشرف | supervisor@gov.iq | Test123! |
| مدير | admin@moamalaty.iq | Admin123! |

## اختبر المنصة! 🎯

### كمواطن:
1. سجل دخول بحساب citizen@test.com
2. اذهب لـ "معاملة جديدة"
3. اختر نوع المعاملة والدائرة
4. قدم المعاملة

### كموظف:
1. سجل دخول بحساب employee@gov.iq
2. شاهد المعاملات الجديدة
3. راجع المعاملات

### كمدير:
1. سجل دخول بحساب admin@moamalaty.iq
2. شاهد جميع الإحصائيات
3. راقب النشاطات

---

## مشاكل شائعة 🔧

### لا يعمل البرنامج؟

**المشكلة**: خطأ في قاعدة البيانات
**الحل**: تأكد من:
- تشغيل PostgreSQL
- صحة DB_PASSWORD في `.env`

**المشكلة**: Port محجوز
**الحل**: 
- أوقف البرنامج الآخر
- أو غير PORT في .env

**المشكلة**: خطأ في التثبيت
**الحل**:
```powershell
rm -r node_modules
npm install
```

---

🎉 **استمتع باستخدام منصة معاملتي!**
